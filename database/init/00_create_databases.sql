-- SPEC v8.4 §19 — executado na primeira subida do volume Postgres (initdb).
-- Cria bancos globais vazios; migrations EF populam schema.

CREATE DATABASE parking_identity;
CREATE DATABASE parking_audit;
